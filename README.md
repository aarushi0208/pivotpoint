PivotPoint AI is a Vite + React app that helps students pivot from failed dreams to new career pathways.

Environment setup:
1. Copy `.env.example` to `.env`.
2. Set `VITE_GEMINI_API_KEY` with your Gemini API key.

Commands:
- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`